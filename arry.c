#include <stdio.h>

int main()
{
    int a,b,ar[5];
    scanf("%d",&a);
    for (size_t i = 0; i < b; i++)
    {
        scanf("%d",&ar[i]);
    }
    for (size_t i = 0; i < b; i++)
    {
        a=a+ar[i];
    }
    printf("%d",a);
    
    return 0;
}